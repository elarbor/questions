import type { Frontmatter, Item, ItemCacheOptions } from "../types";
/** 节流 */
export declare function throttle<T extends (...args: any[]) => void>(func: T, delay: number): (this: ThisParameterType<T>, ...args: Parameters<T>) => void;
/**
 * 获取git时间戳
 * @param absolutePath - 需要是绝对路径，与cache中对应
 */
export declare function getTimestamp(absolutePath: string, isFolder: boolean): Promise<{
    birthTime?: number | undefined;
    modifyTime?: number | undefined;
    firstCommitTime?: number | undefined;
    lastCommitTime?: number | undefined;
}>;
/** 强制重启开发服务器，实现刷新 */
export declare function forceReload(path: string): void;
/** 辅助方法：从 frontmatter 以及 options 中获取实际使用的配置 */
export declare function getTargetOptionValue(frontmatter: Frontmatter, options: ItemCacheOptions, key: string): any;
/** 获取文件夹内子文件、文件夹最小和最大的 git 时间戳 */
export declare function getFolderCommitTimes(children: Item[]): {
    minFirstCommitTime?: number;
    maxLastCommitTime?: number;
};
/** 处理文件夹的 git 时间戳 */
export declare function updateCommitTimes(data: Item[]): void;
